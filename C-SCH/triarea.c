#include<stdio.h>

int main() {
    int h, b;
    printf("enter the hight and base of the traingle \n");
    scanf("%d" "%d", &h, &b);

    int area = 0.5 * h * b;
    printf("traingle area: %d\n", area);

    return 0;
}